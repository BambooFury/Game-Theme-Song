{
  "name": "game-theme-song",
  "version": "2.0.0",
  "description": "Plays a game's theme music in the background on its Steam library page.",
  "author": "BambooFury",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build:assets": "node scripts/build-assets.mjs",
    "dev": "bun run build:assets && starlight watch",
    "build": "bun run build:assets && starlight pack",
    "prepare": "starlight lsp"
  },
  "devDependencies": {
    "@steambrew/starlight": "^1.1.3",
    "@types/react": "19.1.1",
    "@types/react-dom": "19.1.1"
  },
  "dependencies": {
    "react-icons": "^5.7.0"
  }
}